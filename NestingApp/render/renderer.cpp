#include "render/renderer.h"

namespace nest {

// The abstract renderer intentionally has no implementation here. It keeps the
// app ready for a future Direct2D backend without touching geometry or solver code.

} // namespace nest
